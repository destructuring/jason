#!/bin/bash

if [[ -z "${bhome:-}" ]]; then
  bhome="$(basename "$(cd -P -- "$(dirname -- "${BASH_SOURCE}")" && pwd -P)")"
fi

if [[ -z "${shome:-}" ]]; then
  shome="$(dirname "$bhome")"
fi

source "$bhome/_jason"
set -fue
